// props
import Student from "./Student";

const Props = () => {
  const name = "dinesh";
  const roll = "29";
  return (
    <div>
      <Student a={name} roll={roll} />
    </div>
  );
};

export default Props;
